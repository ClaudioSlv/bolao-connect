import Link from "next/link";import {redirect} from "next/navigation";import {createAdminClient} from "@/lib/supabase/admin";export const dynamic="force-dynamic";const money=(c:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(c/100);
async function submitReceipt(f:FormData){"use server";const token=String(f.get("token")??""),file=f.get("receipt");if(!(file instanceof File)||file.size<1)throw new Error("Selecione o comprovante.");if(file.size>5*1024*1024)throw new Error("O arquivo deve ter no máximo 5 MB.");const allowed=["image/jpeg","image/png","image/webp","application/pdf"];if(!allowed.includes(file.type))throw new Error("Envie JPG, PNG, WebP ou PDF.");const s=createAdminClient();const {data:p}=await s.from("participants").select("id,pool_id,status,payment_status").eq("access_token",token).maybeSingle();if(!p||p.status==="cancelled"||p.payment_status==="confirmed")throw new Error("Este pagamento não aceita novo comprovante.");const ext=file.type==="application/pdf"?"pdf":file.type.split("/")[1].replace("jpeg","jpg"),path=`${p.pool_id}/${p.id}/${crypto.randomUUID()}.${ext}`;const {error:u}=await s.storage.from("payment-receipts").upload(path,file,{contentType:file.type,upsert:false});if(u)throw u;await s.from("payment_submissions").update({status:"rejected",reviewed_at:new Date().toISOString()}).eq("participant_id",p.id).eq("status","pending");const {error}=await s.from("payment_submissions").insert({pool_id:p.pool_id,participant_id:p.id,receipt_path:path,status:"pending"});if(error)throw error;redirect(`/p/${token}?sent=1`)}
export default async function Page({params,searchParams}:{params:Promise<{token:string}>;searchParams:Promise<{sent?:string}>}){const {token}=await params,{sent}=await searchParams;let data:any=null;try{const s=createAdminClient();const {data:p}=await s.from("participants").select("id,pool_id,name,shares,status,payment_status").eq("access_token",token).maybeSingle();if(p){const {data:pool}=await s.from("pools").select("title,lottery,share_price_cents,payment_deadline").eq("id",p.pool_id).maybeSingle();const {data:sub}=await s.from("payment_submissions").select("status,created_at").eq("participant_id",p.id).eq("status","pending").order("created_at",{ascending:false}).limit(1).maybeSingle();data={p,pool,sub}}}catch{}if(!data?.p||!data?.pool)return <main className="shell"><section className="section"><h1>Link inválido</h1></section></main>;const {p,pool,sub}=data,amount=Number(p.shares)*Number(pool.share_price_cents),paid=p.payment_status==="confirmed";return <main className="shell"><section className="section"><p className="eyebrow">BOLÃO CONNECT</p><h1>🍀 {pool.title}</h1><p><strong>{p.name}</strong> · {p.shares} cota(s) · <strong>{money(amount)}</strong></p><p className="muted">Prazo: {new Date(pool.payment_deadline).toLocaleString("pt-BR",{timeZone:"America/Sao_Paulo"})}</p></section><section className="section"><h2>Pagamento por Pix</h2><div className="card"><strong>Telefone: 13 99132-0205</strong><span>CPF: 281.649.638-44</span></div>{paid?<p className="status">PAGAMENTO CONFIRMADO</p>:sub||sent?<><p className="status">COMPROVANTE RECEBIDO</p><p className="muted">Aguardando confirmação do organizador.</p></>:<form className="form" action={submitReceipt}><input type="hidden" name="token" value={token}/><div className="field"><label>Enviar comprovante</label><input name="receipt" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required/></div><button className="button primary">Enviar comprovante</button></form>}<p className="muted">O envio do comprovante não confirma o pagamento automaticamente. A confirmação final é feita pelo organizador.</p></section><Link className="back" href="/">Bolão Connect</Link></main>}
