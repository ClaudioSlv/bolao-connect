import Link from "next/link";
import { AppNav } from "@/components/app-nav";
import { createClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
const money=(c:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(c/100);
export default async function Home(){
 const supabase=await createClient();const {data:auth}=await supabase.auth.getUser();let pool:{id:string;title:string;total_shares:number;share_price_cents:number;public_slug:string|null}|null=null;let paidShares=0,pendingShares=0;
 if(auth.user){const {data}=await supabase.from("pools").select("id,title,total_shares,share_price_cents,public_slug").eq("owner_id",auth.user.id).order("created_at",{ascending:false}).limit(1).maybeSingle();pool=data;if(pool){const {data:participants}=await supabase.from("participants").select("shares,payment_status,status").eq("pool_id",pool.id);for(const p of participants??[]){if(p.status==="cancelled")continue;const shares=Number(p.shares)||0;if(p.payment_status==="confirmed")paidShares+=shares;else pendingShares+=shares;}}}
 const collected=pool?paidShares*pool.share_price_cents:0,total=pool?pool.total_shares*pool.share_price_cents:0,progress=total>0?Math.min(100,Math.round(collected/total*100)):0;
 return <main className="shell"><div className="brand"><strong>🍀 Bolão Connect</strong><span className="badge">Online</span></div><section className="hero"><h1>Seu bolão organizado do começo ao resultado.</h1><p className="muted">Crie grupos, distribua cotas, acompanhe pagamentos, publique jogos e deixe todos os participantes acompanharem a arrecadação.</p><div className="actions"><Link className="button primary" href="/criar-bolao">+ Criar bolão</Link><Link className="button secondary" href="/meu-jogo">🎲 Fazer meu próprio jogo</Link><Link className="button secondary" href="/participantes">Participantes</Link>{pool?.public_slug?<Link className="button secondary" href={`/bolao/${pool.public_slug}`}>Abrir link dos participantes</Link>:null}</div></section><section className="section"><h2>Gerenciar bolão</h2><div className="grid"><Link className="card" href="/carteira"><strong>💰 Carteira</strong><span>Arrecadação transparente</span></Link><Link className="card" href="/jogos"><strong>🎟️ Jogos</strong><span>Apostas e comprovantes</span></Link><Link className="card" href="/conferencia"><strong>✅ Conferência</strong><span>Resultados e acertos</span></Link><Link className="card" href="/participantes"><strong>👥 Participantes</strong><span>Cotas e pagamentos</span></Link><Link className="card" href="/meu-jogo"><strong>🎲 Meu Jogo</strong><span>Escolha números ou gere combinações</span></Link></div></section><section className="section"><h2>{pool?pool.title:"Resumo"}</h2>{pool?<div className="wallet"><div className="wallet-row"><strong>Arrecadado</strong><strong>{money(collected)}</strong></div><div className="progress"><div style={{width:`${progress}%`}}/></div><div className="wallet-row"><span>{paidShares} cotas pagas</span><span>{pendingShares} pendentes</span></div><small>Meta: {money(total)} · {progress}% arrecadado</small></div>:<p className="muted">{auth.user?"Você ainda não criou um bolão.":"Entre na sua conta para visualizar os dados do seu bolão."}</p>}</section><AppNav/></main>;
}
