"use client";
import {useMemo,useState} from "react";
type Lottery="mega-sena"|"lotofacil"|"quina"|"dupla-sena"|"lotomania"|"timemania"|"dia-de-sorte"|"super-sete"|"mais-milionaria";type Result={numbers:number[];special_value?:unknown};type Game={numbers:number[];trevos:number[]};
const rules:Record<Lottery,{label:string;min:number;max:number;minPick:number;maxPick:number}>={"mega-sena":{label:"Mega-Sena",min:1,max:60,minPick:6,maxPick:20},lotofacil:{label:"Lotofácil",min:1,max:25,minPick:15,maxPick:20},quina:{label:"Quina",min:1,max:80,minPick:5,maxPick:15},"dupla-sena":{label:"Dupla Sena",min:1,max:50,minPick:6,maxPick:15},lotomania:{label:"Lotomania",min:0,max:99,minPick:50,maxPick:50},timemania:{label:"Timemania",min:1,max:80,minPick:10,maxPick:10},"dia-de-sorte":{label:"Dia de Sorte",min:1,max:31,minPick:7,maxPick:15},"super-sete":{label:"Super Sete",min:0,max:9,minPick:7,maxPick:7},"mais-milionaria":{label:"+Milionária",min:1,max:50,minPick:6,maxPick:12}};const shuffle=<T,>(a:T[])=>{const x=[...a];for(let i=x.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[x[i],x[j]]=[x[j],x[i]];}return x};
const fibonacciUpTo=(max:number)=>{const out=new Set<number>();let a=1,b=2;while(a<=max){out.add(a);[a,b]=[b,a+b]}return out};
export function PersonalGameGenerator({lottery,results}:{lottery:Lottery;results:Result[]}){const r=rules[lottery];const[pick,setPick]=useState(r.minPick),[qty,setQty]=useState(1),[manual,setManual]=useState<number[]>([]),[games,setGames]=useState<Game[]>([]),[excludedOdd,setExcludedOdd]=useState<number[]>([]),[excludedEven,setExcludedEven]=useState<number[]>([]),[trevoPick,setTrevoPick]=useState(2),[manualTrevos,setManualTrevos]=useState<number[]>([]),[saved,setSaved]=useState(false),[shared,setShared]=useState(false);const allNumbers=useMemo(()=>Array.from({length:r.max-r.min+1},(_,i)=>r.min+i),[r.min,r.max]);const oddNumbers=useMemo(()=>allNumbers.filter(n=>n%2!==0),[allNumbers]);const evenNumbers=useMemo(()=>allNumbers.filter(n=>n%2===0),[allNumbers]);const exclusionLimit=Math.max(1,Math.floor((r.max-r.min+1)/4));const excluded=new Set([...excludedOdd,...excludedEven]);const available=allNumbers.filter(n=>!excluded.has(n));const fibSet=useMemo(()=>fibonacciUpTo(r.max),[r.max]);const stats=useMemo(()=>{if(lottery==="super-sete"){const cols=Array.from({length:7},()=>Array(10).fill(0));for(const d of results.slice(0,50))d.numbers?.slice(0,7).forEach((n,i)=>{if(Number.isInteger(n)&&n>=0&&n<=9)cols[i][n]++});return{cols,freq:new Map<number,number>(),delay:new Map<number,number>()}}const freq=new Map<number,number>(),delay=new Map<number,number>();for(let n=r.min;n<=r.max;n++){freq.set(n,0);delay.set(n,results.length)}for(const [index,d] of results.slice(0,50).entries())for(const n of d.numbers??[]){freq.set(n,(freq.get(n)??0)+1);if(delay.get(n)===results.length)delay.set(n,index)}return{freq,delay,cols:[]}},[results,lottery,r.min,r.max]);const ordered=useMemo(()=>lottery==="super-sete"?[]:[...stats.freq.entries()].sort((a,b)=>b[1]-a[1]).map(([n])=>n),[stats,lottery]);const delayed=useMemo(()=>lottery==="super-sete"?[]:[...stats.delay.entries()].sort((a,b)=>b[1]-a[1]).map(([n])=>n),[stats,lottery]);
const oneNumbers=()=>{if(lottery==="super-sete")return stats.cols.map(c=>{const m=Math.max(...c,1),w:number[]=[];for(let d=0;d<=9;d++)for(let i=0;i<1+Math.round(c[d]/m*5);i++)w.push(d);return w[Math.floor(Math.random()*w.length)]});const allowed=new Set(available),ranked=ordered.filter(n=>allowed.has(n)),hot=ranked.slice(0,Math.max(1,Math.ceil(ranked.length*.33))),cold=ranked.slice(Math.floor(ranked.length*.67)),late=delayed.filter(n=>allowed.has(n)).slice(0,Math.max(1,Math.ceil(available.length*.33))),fibs=available.filter(n=>fibSet.has(n)),chosen=new Set<number>();const take=(a:number[],c:number)=>shuffle(a.filter(n=>!chosen.has(n))).slice(0,c).forEach(n=>chosen.add(n));if(results.length){take(hot,Math.max(1,Math.round(pick*.30)));take(cold,Math.max(1,Math.round(pick*.18)));take(late,Math.max(1,Math.round(pick*.18)));take(fibs,Math.max(1,Math.round(pick*.20)))}else take(fibs,Math.max(1,Math.round(pick*.20)));for(const n of shuffle(available))if(chosen.size<pick)chosen.add(n);return[...chosen].slice(0,pick).sort((a,b)=>a-b)};const oneTrevos=()=>lottery==="mais-milionaria"?(manualTrevos.length===trevoPick?[...manualTrevos]:shuffle([1,2,3,4,5,6]).slice(0,trevoPick).sort((a,b)=>a-b)):[];const canGenerate=lottery==="super-sete"||available.length>=pick;const resetFeedback=()=>{setSaved(false);setShared(false)};const showGenerated=()=>setTimeout(()=>document.getElementById("generated-games")?.scrollIntoView({behavior:"smooth",block:"start"}),80);const auto=()=>{if(canGenerate){const wanted=Math.max(1,Math.min(1000,qty)),out:Game[]=[],seen=new Set<string>();let attempts=0;while(out.length<wanted&&attempts<wanted*30){attempts++;const game={numbers:oneNumbers(),trevos:oneTrevos()},key=`${game.numbers.join("-")}|${game.trevos.join("-")}`;if(!seen.has(key)){seen.add(key);out.push(game)}}setGames(out);resetFeedback();showGenerated()}};const buildManualGames=(numbers:number[])=>{if(numbers.length!==pick)return;setGames(Array.from({length:Math.max(1,Math.min(1000,qty))},()=>({numbers:[...numbers],trevos:oneTrevos()})));resetFeedback();showGenerated()};const toggle=(n:number)=>setManual(v=>{const next=v.includes(n)?v.filter(x=>x!==n):v.length<pick?[...v,n].sort((a,b)=>a-b):v;if(next.length===pick)buildManualGames(next);else setGames([]);return next});const toggleTrevo=(n:number)=>{setManualTrevos(v=>v.includes(n)?v.filter(x=>x!==n):v.length<trevoPick?[...v,n].sort((a,b)=>a-b):v);setGames([]);resetFeedback()};const toggleExclude=(n:number,odd:boolean)=>{const current=odd?excludedOdd:excludedEven;const setter=odd?setExcludedOdd:setExcludedEven;if(current.includes(n)){setter(current.filter(x=>x!==n));return}if(current.length>=exclusionLimit||available.length-1<pick)return;setter([...current,n].sort((a,b)=>a-b));setManual(v=>v.filter(x=>x!==n));setGames([]);resetFeedback()};const manualGames=()=>buildManualGames(manual);const setValidPick=(raw:string)=>{const n=Number(raw);if(!Number.isFinite(n))return;const valid=Math.max(r.minPick,Math.min(r.maxPick,Math.trunc(n)));setPick(valid);setManual([]);setGames([]);setExcludedOdd([]);setExcludedEven([]);resetFeedback()};const gameText=(game:Game,i:number)=>`Jogo ${i+1}: ${game.numbers.map(n=>String(n).padStart(2,"0")).join(" · ")}${lottery==="mais-milionaria"?` | Trevos: ${game.trevos.map(n=>String(n).padStart(2,"0")).join(" · ")}`:""}`;const shareText=()=>`🍀 ${r.label} — Bolão Amigos BTP\n\n${games.map(gameText).join("\n")}\n\nJogo gerado pelo Bolão Amigos BTP.`;const saveGames=()=>{try{const key="bolao-amigos-btp:jogos-salvos";const current=JSON.parse(localStorage.getItem(key)||"[]");const entry={id:`${Date.now()}-${Math.random().toString(36).slice(2)}`,lottery,label:r.label,games,createdAt:new Date().toISOString()};localStorage.setItem(key,JSON.stringify([entry,...(Array.isArray(current)?current:[])]));setSaved(true);setTimeout(()=>setSaved(false),3000)}catch{alert("Não foi possível salvar este jogo neste aparelho.")}};const shareGames=async()=>{const text=shareText();try{if(navigator.share){await navigator.share({title:`${r.label} — Bolão Amigos BTP`,text});setShared(true);setTimeout(()=>setShared(false),3000);return}if(navigator.clipboard){await navigator.clipboard.writeText(text);setShared(true);setTimeout(()=>setShared(false),3000);return}window.location.href=`https://wa.me/?text=${encodeURIComponent(text)}`}catch(e){if((e as Error)?.name!=="AbortError")window.location.href=`https://wa.me/?text=${encodeURIComponent(text)}`}};
return <div className="form"><div className="field"><label>Números por jogo <small className="muted">(mín. {r.minPick} · máx. {r.maxPick})</small></label><input type="number" inputMode="numeric" min={r.minPick} max={r.maxPick} step="1" value={pick} onFocus={e=>e.currentTarget.select()} onChange={e=>setValidPick(e.target.value)}/></div>{lottery==="mais-milionaria"&&<div className="section"><h3>🍀 Trevos da +Milionária</h3><p className="muted">Escolha os trevos de 01 a 06. Na aposta simples são 2 trevos. Se não marcar, o sistema escolhe automaticamente.</p><div className="field"><label>Trevos por jogo <small className="muted">(mín. 2 · máx. 6)</small></label><input type="number" inputMode="numeric" min="2" max="6" value={trevoPick} onFocus={e=>e.currentTarget.select()} onChange={e=>{const n=Math.max(2,Math.min(6,Math.trunc(Number(e.target.value)||2)));setTrevoPick(n);setManualTrevos(v=>v.slice(0,n));setGames([]);resetFeedback()}}/></div><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:"12px",maxWidth:"360px"}}>{[1,2,3,4,5,6].map(n=>{const selected=manualTrevos.includes(n);return <button key={n} type="button" onClick={()=>toggleTrevo(n)} aria-pressed={selected} style={{fontSize:"30px",minHeight:"76px",border:"0",background:"transparent",cursor:"pointer",filter:selected?"drop-shadow(0 0 10px #f7c948)":"drop-shadow(0 2px 3px #000)"}}><span style={{position:"relative",display:"inline-flex",alignItems:"center",justifyContent:"center",width:"64px",height:"64px",fontSize:"62px",lineHeight:1,color:selected?"#f7c948":"#45d66b",textShadow:selected?"0 0 10px rgba(247,201,72,.8)":"0 0 7px rgba(69,214,107,.45)"}}>☘<b style={{position:"absolute",fontSize:"15px",color:selected?"#111":"white",textShadow:selected?"none":"0 1px 3px #000"}}>{String(n).padStart(2,"0")}</b></span></button>})}</div><p className="muted">Trevos selecionados: {manualTrevos.length}/{trevoPick}</p></div>}<div className="field"><label>Quantidade de jogos <small className="muted">(máx. 1000)</small></label><input type="number" inputMode="numeric" min="1" max="1000" value={qty} onFocus={e=>e.currentTarget.select()} onChange={e=>{const n=Number(e.target.value);if(Number.isFinite(n))setQty(Math.max(1,Math.min(1000,Math.trunc(n))))}}/></div>{lottery!=="super-sete"&&<div className="section"><h3>🚫 Números que NÃO quero nos jogos</h3><p className="muted">Opcional. Escolha até {exclusionLimit} ímpares e até {exclusionLimit} pares. Os selecionados ficam fora de todos os jogos.</p><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:"12px"}}><div><strong>1 - ÍMPAR ({excludedOdd.length}/{exclusionLimit})</strong><div className="number-grid" style={{gridTemplateColumns:"repeat(3,1fr)",marginTop:"10px"}}>{oddNumbers.map(n=><button key={n} type="button" className={`number-button ${excludedOdd.includes(n)?"selected":""}`} onClick={()=>toggleExclude(n,true)}>{String(n).padStart(2,"0")}</button>)}</div></div><div><strong>2 - PAR ({excludedEven.length}/{exclusionLimit})</strong><div className="number-grid" style={{gridTemplateColumns:"repeat(3,1fr)",marginTop:"10px"}}>{evenNumbers.map(n=><button key={n} type="button" className={`number-button ${excludedEven.includes(n)?"selected":""}`} onClick={()=>toggleExclude(n,false)}>{String(n).padStart(2,"0")}</button>)}</div></div></div></div>}<div className="actions"><button className="button primary" type="button" onClick={auto} disabled={!canGenerate}>🎲 Gerar fechamento</button>{lottery!=="super-sete"&&<button className="button secondary" type="button" onClick={manualGames} disabled={manual.length!==pick}>Usar meus números</button>}</div>{lottery!=="super-sete"&&<div><p className="muted">Escolha manualmente {pick} números:</p><div className="number-grid">{available.map(n=><button key={n} type="button" className={`number-button ${manual.includes(n)?"selected":""}`} onClick={()=>toggle(n)}>{String(n).padStart(2,"0")}</button>)}</div></div>}<p className="muted">Base do fechamento: últimos {Math.min(50,results.length)} resultados salvos · números quentes · frios · atrasados · Fibonacci. Os números excluídos nunca entram nos jogos gerados.</p>{games.length>0&&<div className="section" id="generated-games" style={{scrollMarginTop:"150px"}}><h2>Seus jogos</h2><div className="list">{games.map((g,i)=><div className="list-item" key={i}><strong>Jogo {i+1}</strong><span>{g.numbers.map(n=>String(n).padStart(2,"0")).join(" · ")}{lottery==="mais-milionaria"&&<> <b>🍀 Trevos:</b> {g.trevos.map(n=>String(n).padStart(2,"0")).join(" · ")}</>}</span></div>)}</div><div className="actions" style={{marginTop:"16px"}}><button className="button primary" type="button" onClick={saveGames}>{saved?"✅ JOGO SALVO":"💾 SALVAR JOGO"}</button><button className="button secondary" type="button" onClick={shareGames}>{shared?"✅ PRONTO":"📲 COMPARTILHAR JOGO"}</button></div><p className="muted" style={{fontSize:"12px"}}>Salvar guarda os jogos neste aparelho. Compartilhar abre o menu do celular para enviar pelo WhatsApp ou outro aplicativo.</p></div>}</div>}
