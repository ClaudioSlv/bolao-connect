"use client";
import {useState} from "react";
export type ReceiptPage={id:string;url:string;order:number};
export function ReceiptViewer({title,pages}:{title:string;pages:ReceiptPage[]}){const[open,setOpen]=useState(false);return <div className="receipt-document"><button type="button" className="receipt-open" onClick={()=>setOpen(true)}><span className="receipt-thumb">{pages[0]?<img src={pages[0].url} alt="Prévia do comprovante"/>:null}</span><span><strong>{title}</strong><small>{pages.length} parte(s) · toque para conferir</small></span><b>›</b></button>{open&&<div className="receipt-viewer-overlay" role="dialog" aria-modal="true" aria-label={title}><header><strong>{title}</strong><button type="button" onClick={()=>setOpen(false)} aria-label="Fechar">×</button></header><p>Deslize para cima e para baixo. Use dois dedos para ampliar.</p><div className="receipt-scroll">{pages.map((page,index)=><img key={page.id} src={page.url} alt={`Parte ${index+1} do comprovante`} loading={index?"lazy":"eager"}/>)}</div></div>}</div>}
