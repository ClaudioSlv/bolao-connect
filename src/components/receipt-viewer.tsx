"use client";
import {useState} from "react";
export function ReceiptViewer({title,url}:{title:string;url:string}){const[open,setOpen]=useState(false);return <div className="receipt-document"><button type="button" className="receipt-open" onClick={()=>setOpen(true)}><span className="receipt-thumb"><img src={url} alt="Prévia do comprovante"/></span><span><strong>{title}</strong><small>Toque para ampliar e movimentar</small></span><b>›</b></button>{open&&<div className="receipt-viewer-overlay" role="dialog" aria-modal="true" aria-label={title}><header><strong>{title}</strong><button type="button" onClick={()=>setOpen(false)} aria-label="Fechar">×</button></header><p>Use dois dedos para ampliar. Arraste em qualquer direção.</p><div className="receipt-pan"><img src={url} alt={title}/></div></div>}</div>}
